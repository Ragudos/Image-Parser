{
  "name": "@image-parser/web",
  "description": "The website that showcases the stuff in @image-parser.",
  "version": "1.0.0",
  "scripts": {
    "clean": "rmdir /s /q public\\dist && del /s /q tsconfig.tsbuildinfo",
    "test": "echo 'No tests for web right now'",
    "build": "node esbuild.config.mjs",
    "build:types": "tsc",
    "lint": "eslint ./src",
    "lint:fix": "eslint ./src --fix",
    "format": "prettier --write ./src"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  },
  "keywords": [
    "Image parsing"
  ],
  "author": {
    "email": "workemailaaronragudos@gmail.com",
    "name": "Aaron Ragudos"
  },
  "repository": {
    "url": "https://github.com/Ragudos/Image-Parser.git",
    "type": "Github"
  },
  "license": "CC BY 4.0",
  "dependencies": {
    "@image-parser/core": "^1.0.0"
  }
}